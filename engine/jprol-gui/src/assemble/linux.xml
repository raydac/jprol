<assembly xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2 http://maven.apache.org/xsd/assembly-1.1.2.xsd">
    <id>bundle</id>

    <includeBaseDirectory>false</includeBaseDirectory>
    <includeSiteDirectory>false</includeSiteDirectory>

    <formats>
        <format>tar.gz</format>
    </formats>
    <files>
        <file>
            <source>${project.build.directory}${file.separator}${app.name}.jar</source>
            <destName>${app.name}.jar</destName>
            <outputDirectory>${file.separator}</outputDirectory>
        </file>
        <file>
            <filtered>true</filtered>
            <source>
                ${project.basedir}${file.separator}src${file.separator}assemble${file.separator}linux${file.separator}run.sh
            </source>
            <outputDirectory>${file.separator}</outputDirectory>
        </file>
        <file>
            <source>${project.build.directory}${file.separator}${app.name}.jar</source>
            <destName>${app.name}.jar</destName>
            <outputDirectory>${file.separator}</outputDirectory>
        </file>
    </files>
    <fileSets>
        <fileSet>
            <directory>${project.basedir}${file.separator}src${file.separator}assemble${file.separator}linux</directory>
            <outputDirectory>${file.separator}</outputDirectory>
            <filtered>false</filtered>
            <excludes>
                <exclude>**/*.sh</exclude>
            </excludes>
            <includes>
                <include>*/**</include>
            </includes>
        </fileSet>
        <fileSet>
            <directory>${project.build.directory}</directory>
            <outputDirectory>${file.separator}</outputDirectory>
            <includes>
                <include>${artifactId}-${version}.${packaging}</include>
            </includes>
        </fileSet>
    </fileSets>
</assembly>